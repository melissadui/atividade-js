// função chamada somar que receba dois números como parâmetros e retorne a soma entre eles.

function somar(a, b) {
    let soma = a + b;
    console.log(`A soma de ${a} e ${b} é ${soma}`);
}

somar(20, 5);